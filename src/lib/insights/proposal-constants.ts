/** Minimum assistant reply length before insight proposals run (noise gate). */
export const MIN_ASSISTANT_MESSAGE_LENGTH = 80;

/** Max proposals returned from extraction. */
export const MAX_PROPOSALS = 3;

/** Chips shown before "+N more" expand control. */
export const VISIBLE_CHIP_LIMIT = 3;

/** Confidence below this is de-emphasized in the proposal bar. */
export const MEDIUM_CONFIDENCE_THRESHOLD = 0.7;
